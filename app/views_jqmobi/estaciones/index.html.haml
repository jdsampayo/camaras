%ul
  - @estaciones.each do |estacion|
    %li
      %a{href: estacion_path(estacion)} , class: "linea-activa-#{linea.nombre}"
        %img.estacion{src: "/images/#{estacion.imagen}"}
        =estacion.nombre

