%header{class: "linea-7"}
  %h2{class: "linea-7"} LINEAS

%ul
  - @lineas.each do |linea|
    %li
      %a{href: "#linea#{linea.nombre}", class: "linea-activa-#{linea.nombre}"}      
        =linea.nombre + " : " + linea.estaciones.first.nombre + " - " + linea.estaciones.last.nombre
